package {{.PackageName}}_test

import (
	"testing"

	"{{.ModuleName}}/internal/core/domain/{{.PackageName}}"
	"github.com/stretchr/testify/assert"
	"github.com/stretchr/testify/require"
)

func TestNew{{.EntityName}}(t *testing.T) {
	tests := []struct {
		name    string
		wantErr bool
	}{
		{
			name:    "valid {{.EntityName}}",
			wantErr: false,
		},
		// TODO: Add more test cases
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			// Arrange & Act
			entity, err := {{.PackageName}}.New{{.EntityName}}( /* TODO: Add parameters */ )

			// Assert
			if tt.wantErr {
				require.Error(t, err)
				assert.Nil(t, entity)
			} else {
				require.NoError(t, err)
				assert.NotNil(t, entity)
			}
		})
	}
}

func Test{{.EntityName}}_Validate(t *testing.T) {
	tests := []struct {
		name    string
		entity  *{{.PackageName}}.{{.EntityName}}
		wantErr bool
	}{
		// TODO: Add test cases
	}

	for _, tt := range tests {
		t.Run(tt.name, func(t *testing.T) {
			err := tt.entity.Validate()
			if tt.wantErr {
				assert.Error(t, err)
			} else {
				assert.NoError(t, err)
			}
		})
	}
}
