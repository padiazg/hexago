package mapper

import (
	"{{.ModuleName}}/internal/core/domain"
)

// {{.Name}} {{.Description}}
type {{.Name}} struct {
	// TODO: Add dependencies if needed
}

// New{{.Name}} creates a new {{.Name}} instance
func New{{.Name}}() *{{.Name}} {
	return &{{.Name}}{}
}

// ToDTO converts a domain entity to a DTO
func (m *{{.Name}}) ToDTO(entity interface{}) interface{} {
	// TODO: Implement domain to DTO mapping
	// Example:
	// if user, ok := entity.(*domain.User); ok {
	//     return &UserDTO{
	//         ID:    user.ID,
	//         Name:  user.Name,
	//         Email: user.Email,
	//     }
	// }
	return nil
}

// ToDomain converts a DTO to a domain entity
func (m *{{.Name}}) ToDomain(dto interface{}) (interface{}, error) {
	// TODO: Implement DTO to domain mapping
	// Example:
	// if userDTO, ok := dto.(*UserDTO); ok {
	//     return domain.NewUser(
	//         userDTO.ID,
	//         userDTO.Name,
	//         userDTO.Email,
	//     )
	// }
	return nil, nil
}

// ToListDTO converts a slice of domain entities to DTOs
func (m *{{.Name}}) ToListDTO(entities []interface{}) []interface{} {
	dtos := make([]interface{}, 0, len(entities))
	for _, entity := range entities {
		dtos = append(dtos, m.ToDTO(entity))
	}
	return dtos
}
